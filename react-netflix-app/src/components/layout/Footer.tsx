import styled from 'styled-components';
import { SCREEN_SIZE } from '@constants/screen.constant';

export function Footer() {
  return (
    <Container>

    </Container>
  );
}

const Container = styled.footer`
  width: ${SCREEN_SIZE}
`;